To build the exe, from the commandline, run `build.ps1`. 
This will generate an exe at the root directory of the project. 

To run the exe, just do `HLTVPredictionsWebScrapper.exe /link/to/predictionpage` 
